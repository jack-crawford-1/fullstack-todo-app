export interface Fruit {
  id: number
  name: string
}

export interface FruitData {
  name: string
}

export interface Todo {
  id: number
  task: string
}

// TODO 1 define type of data in an interface
//Define the data type (models/todos.ts: Shape of the data.
